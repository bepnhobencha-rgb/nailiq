# Audit Report — 2026-05-04

> Scope: full-tree audit of `nailiq` (branch `yolo-mode`).
> Method: `npm run typecheck` / `npm run lint` / `npm run build` plus manual review of `src/`, `supabase/migrations/`, `e2e/`, `.github/workflows/`, `scripts/`.
> Status: **report only — no fixes applied.**
>
> **Resolution note (2026-07-23):** C-2 is resolved by the folded-history
> cutover and production ledger repair in PRs #912/#913. Production and the
> checked-in history are at strict 267/267 parity; the original row below is
> retained as historical evidence.

## Stats
- Source files scanned (`src/**/*.{ts,tsx}`): **185**
- Migrations scanned: **24**
- E2E spec files: **22** (no local run performed in this audit; CI has historically been green per `.github/workflows/e2e.yml` after commit `06e9463`)
- `npm run typecheck`: **passes (0 errors)**
- `npm run build`: **passes (Next 16.2.4, 2.8 s compile)** — emits one deprecation warning (see H-3)
- `npm run lint`: **236 errors, 2848 warnings** — but the vast majority come from ESLint scanning **`playwright-report/` minified bundles** and a stale **`.claude/worktrees/bold-goldberg-f21cae/`** copy of `src/`. Real `src/` findings are confined to ~17 lines across 12 files (see C-3, M-7).

## Critical (fix immediately)

| # | Issue | File | Risk | Effort |
|---|---|---|---|---|
| C-1 | **Demo-cookie path bypasses RLS via service-role client without an independent membership check.** When `kind === "demo_cookie"`, `writableSupabase` returns `createServiceRoleClient()` unconditionally (no `isDemoOtpRuntime()` re-check at this branch). All authorization rests on the cookie-slug ↔ URL-slug string match in `resolveSalonForDashboard`. If `DEMO_OTP`/`NEXT_PUBLIC_DEMO_OTP` is ever set to `true` in production (the toggle is runtime-readable on Vercel), any visitor who completes the demo OTP flow gets a 24h httpOnly cookie that grants service-role writes to that salon's tables. | `src/shared/dashboard/setupActions.ts:42-58`; cookie set at `src/shared/register/completeSalonRegistrationAction.ts:194-200`; matcher in `src/middleware.ts:65-71` | Full RLS bypass on `salons`, `services`, `staff`, `bookings` for any attacker who can reach demo OTP. | M — collapse the demo path to require `isDemoOtpRuntime()` AND verify `salon_members` server-side; or refuse the `demo_cookie` kind when `NODE_ENV === "production"`. |
| C-2 (resolved) | **Historical: Supabase migration tracking was desynced from production.** See the resolution note above and `docs/audit/MIGRATION-HISTORY-RECONCILIATION-2026-07-23.md`. | `supabase/migrations/*` vs. remote tracker | Resolved; retained for audit history. | Completed in PRs #912/#913. |
| C-3 | **Two ESLint errors (Toast.tsx) — refs accessed during render & setState in effect.** `onDismissRef.current = onDismiss` runs during render, and `setOpen(false)` fires synchronously inside `useEffect`. React 19 strict mode + the React-Compiler / `react-hooks/refs` rule treat both as bugs that can drop updates. | `src/components/ui/Toast.tsx:31`, `src/components/ui/Toast.tsx:44` | Toast dismiss can become stale or render-loop in production builds. | S — move ref assignment into `useEffect`, gate `setOpen(false)` on `open` value or move into the timer callback. |

## High (fix this week)

| # | Issue | File | Risk | Effort |
|---|---|---|---|---|
| H-1 | **`scripts/auto-push.js` has no typecheck/build/test gate** — debounces 30 s after any save and pushes. CLAUDE.md flags it as gated behind a permission prompt, but the script itself does not block on red builds. | `scripts/auto-push.js` (full file) | Broken code can land on `yolo-mode` (and `main` if reused) without any preflight. | S — wrap the push step in `npm run typecheck && npm run build`. |
| H-2 | **Register flow stores OTP completion token + phone in `sessionStorage`.** `REG_COMPLETION_TOKEN_KEY` is the proof that an OTP was verified. Anything reachable via XSS can read it and impersonate the registrant. CLAUDE.md already lists this as a Phase-2 candidate; it remains unmoved. | `src/shared/lib/registerSessionKeys.ts`; consumed in `src/app/register/RegisterPageClient.tsx`, `src/app/register/verify/VerifyPageClient.tsx`, `src/app/register/setup/RegisterSetupInner.tsx` | Token theft via XSS = full register-as-victim. | M — move to httpOnly cookie set by the verify action; clear after `setup` completes. |
| H-3 | **Next 16 deprecation: `middleware.ts` → `proxy.ts`.** Build emits "The 'middleware' file convention is deprecated. Please use 'proxy' instead." | `src/middleware.ts`; build log reference. | Will hard-break on a future minor release of Next. | S — rename file, update import paths and `config.matcher`. |
| H-4 | **`client_profiles` RLS is effectively public-write.** Migration installs `for insert with check (true)` and `for update using (true)` — any anonymous client can overwrite *any* phone-keyed profile row (including `preferred_staff_id`, `visit_count`, `display_name`). | `supabase/migrations/20260430180000_client_profiles.sql:15-25` | CRM data corruption across all salons by anyone hitting the public booking endpoint. | M — scope updates to "phone matches the booking the request is creating" via SECURITY DEFINER RPC, or restrict update to service-role only. |
| H-5 | **No application-level Origin/Referer validation on Server Actions.** Next provides default action protections but the codebase relies entirely on them — no defence-in-depth check on the dashboard mutations that hold service-role credentials in the demo path. | `src/shared/dashboard/setupActions.ts`, `salonOwnerActions.ts`, `receptionistActions.ts`, `editBookingAction.ts` | If the Next default ever loosens or an action is repurposed into a route handler, CSRF surface opens. | S — add a small helper that asserts `headers().get('origin')` matches `getSiteUrl()` in mutation paths. |
| H-6 | **Missing indexes on hot booking queries.** No index on `bookings (salon_id, start_time_utc)` or `(salon_id, staff_id, start_time_utc)`; no index on `services (salon_id)` or `staff (salon_id)`. The receptionist day grid (`loadReceptionistCenterData.ts:200-221`) and public-booking occupancy RPC scan sequentially. | `supabase/migrations/*`; queries in `src/shared/dashboard/loadReceptionistCenterData.ts:200-221`, `src/shared/booking/submitPublicBooking.ts:165-169, 258-262` | Latency grows linearly with bookings; problem worsens once any salon scales past a few hundred rows. | S — single migration adds 4 b-tree / partial indexes (see report appendix). |
| H-7 | **Two real lint errors in receptionist + booking flow components.** `setState` directly in `useEffect` body (cascading-render rule). | `src/components/receptionist/ReceptionistCenter.tsx:196`; `src/components/booking/BookingFlowDatePanel.tsx:107` | Real cascading-render risk under React 19 / the React Compiler optimizer. | S — wrap in conditional or move into the synchronization callback. |
| H-8 | **Phase-2 email verification is silently a no-op.** `addEmailAction.ts` writes the email but the comment block at `:36-39` makes clear the Resend send call is not wired. End-users will see "verification sent" UI with nothing actually sent. | `src/shared/dashboard/addEmailAction.ts:36, 39` | Misleading UX; failed deliverability with no signal. | M — wire Resend or remove the verification copy from the UI. |

## Medium (fix this month)

| # | Issue | File | Risk | Effort |
|---|---|---|---|---|
| M-1 | **Files > 500 LOC need a split.** `ReceptionistCenter.tsx` (959), `useBookingFlowState.ts` (781), `setupActions.ts` (765), `submitPublicBooking.ts` (519). All have natural concern boundaries (queue vs. grid; per-step state vs. conflict detection; per-entity actions; RPC vs. profile sync). | listed paths | Slow review, hard-to-test, lint hot spots. | M-L per file. |
| M-2 | **Duplicated phone-validation rules.** Four functions enforce slightly different digit ranges (8-15 vs. 10-15). | `src/shared/dashboard/addressSetupValidation.ts:17-20`, `src/shared/lib/phoneFormat.ts:56-62`, `src/shared/register/phone.ts:24-27`, `src/shared/booking/validateGuestPhone.ts:10` | Future rule drift; bug already latent (different errors for the same number on different surfaces). | S — promote one canonical helper; update three call sites. |
| M-3 | **No `loading.tsx` / `error.tsx` boundaries on most routes.** Only `/dashboard/[slug]` has a `loading.tsx`; no `error.tsx` exists anywhere under `src/app/`. | `src/app/**` | White-screen on data fetch / runtime error in 13 of 14 routes. | S — add boundaries with the existing `Toast`/`Skeleton` primitives. |
| M-4 | **No focus management in dialogs.** `BookingDetailDrawer` has `aria-modal="true"` and Escape handling, but no focus trap or restore-on-close. `DemoOtpModal` has the same gap. | `src/components/receptionist/BookingDetailDrawer.tsx:152-160`, `src/components/register/DemoOtpModal.tsx:44-46` | Keyboard users tab out of the drawer; assistive tech reads background content. | S — small `useEffect` that focuses the first focusable child and restores `previouslyFocusedElement` on close. |
| M-5 | **Pre-flight overlap check duplicated in RPC + GIST EXCLUDE constraint + client `conflictCheck.ts`.** All three are intentional (UX vs. RPC error vs. race safety) but undocumented; future maintainer is likely to "simplify" one. | `src/shared/lib/conflictCheck.ts`; migrations `20260430230000_bookings_no_overlap_gist.sql`, `20260502130000_public_booking_conflict_check.sql` | Risk of accidental regression to single-layer protection. | XS — comment block in `conflictCheck.ts` documenting the three-layer model. |
| M-6 | **`react-hooks/exhaustive-deps` warnings in receptionist + booking.** Missing `undoState` dep at `ReceptionistCenter.tsx:238`, missing `dayClosed` dep at `BookingFlowDatePanel.tsx:142`. Several more in `useBookingFlowState.ts`. | listed paths | Stale closures around timers / async callbacks. | S each — add deps or refs. |
| M-7 | **ESLint config does not ignore `playwright-report/`, `.claude/worktrees/`, or `scripts/auto-push.js` `require()` style.** Inflates the CI lint output to 3084 problems and obscures real findings. CI has `continue-on-error: true` so this hides regressions. | `eslint.config.mjs`; `globalIgnores` only covers `.next/**`, `out/**`, `build/**`, `next-env.d.ts` | Real lint regressions are invisible in noise. | S — add `playwright-report/**`, `.claude/**`, `test-results/**` to `globalIgnores`; either ignore `scripts/*.js` or convert to ESM. |
| M-8 | **Stale worktree under `.claude/worktrees/bold-goldberg-f21cae/`** — full duplicated copy of `src/` from a prior agent session that never cleaned up. ~30 files. Inflates lint, search results, and bundle scans. | `.claude/worktrees/bold-goldberg-f21cae/**` | Confused agents and future diffs. | XS — delete after confirming no in-progress work; gitignore `.claude/worktrees/`. |
| M-9 | **`use client` over-applied.** Spot-checked: `src/components/receptionist/StatusPill.tsx` is a pure functional component with no hooks/handlers but is `"use client"`. Need a sweep across the 44 client-marked components. | `src/components/receptionist/StatusPill.tsx` (and others) | Larger client bundle; slower hydration on the booking page. | S per file once the inventory is built. |
| M-10 | **Magic numbers.** Status feedback timeouts (3000 ms across 5 dashboard panels), receptionist time-tick (60_000 ms), shake message (2400 ms), undo tick (1000 ms). | `src/components/receptionist/ReceptionistCenter.tsx:177, 233, 244` plus dashboard panels | Drift across panels (some are 3000, some are 2400). | XS — single `src/components/receptionist/timing.ts` (or co-located `const`). |
| M-11 | **CI lint step has `continue-on-error: true`.** The pipeline never fails on lint; combined with M-7 noise, real issues land silently. | `.github/workflows/ci.yml` (lint job) | Regressions accumulate. | XS — flip to `false` after M-7. |
| M-12 | **Demo OTP behaviour is not pinned by tests.** No unit test for `isDemoOtpRuntime()` covering the env permutations; no E2E that asserts the demo modal is *not* rendered when `DEMO_OTP=false`. | `src/shared/lib/demoOtpMode.ts`; missing in `e2e/` | Silent regression to "demo on in prod" remains possible. | S — add a unit test in `src/shared/lib/__tests__/`. |

## Low (nice to have)

| # | Issue | File | Risk | Effort |
|---|---|---|---|---|
| L-1 | **`dangerouslySetInnerHTML` for JSON-LD.** Content is server-built static schema; `JSON.stringify` is escape-safe. Documented for completeness — not exploitable today. | `src/app/page.tsx:45` | None as written. | n/a |
| L-2 | **Two migration files share timestamp `20260430240000`** (`create_public_booking_atomic_json.sql` and `salon_slug_similarity_rpc.sql`). Apply order is filesystem-alphabetical. Already noted in `docs/decisions.md`. | `supabase/migrations/20260430240000_*` | Future maintainer confusion. | XS — rename one to `…241000_…`. |
| L-3 | **`bookings_client_name_safe` constraint added with `NOT VALID`.** Existing rows are not validated. | `supabase/migrations/20260503210000_bookings_client_name_check.sql` | Bad legacy data still allowed. | XS — `ALTER TABLE … VALIDATE CONSTRAINT` once data is clean. |
| L-4 | **`create_public_booking` RPC v2.0 fallback is still in code.** Comment at `submitPublicBooking.ts:404-406` notes "remove once migration is universal." | `src/shared/booking/submitPublicBooking.ts:404-406` | Dead branch; small bundle bloat; misleading. | XS — delete branch once you confirm v2.4 is everywhere. |
| L-5 | **Unused symbol** `CLIENT_NOTES_MAX_LEN` in `receptionistActions.ts:22`. Lint flags it. | `src/shared/dashboard/receptionistActions.ts:22` | Dead code. | XS. |
| L-6 | **Public-booking occupancy RPC excludes `in_progress` and `completed`.** Correct for "is this slot bookable", potentially surprising to a future caller. | `supabase/migrations/20260430180000_public_booking_occupancy_rpc.sql` | Foot-gun for new callers. | XS — comment in RPC body. |
| L-7 | **`BookingParams` / `BookingResult` not exported.** Forces re-declaration if another module wants to call `submitPublicBooking`. | `src/shared/booking/submitPublicBooking.ts:13-27` | DRY drift. | XS. |
| L-8 | **Top-level docs sprawl.** `current-focus.md`, `decisions-log.md`, `DECISIONS.md` (79 B), `PROJECT_BRAIN.md` (31 KB), `PROMPTS_MASTER.md` (59 B), `ROADMAP.md`, `TEST_PLAN.md`, `CHANGELOG.md` (50 KB) at repo root. CLAUDE.md says decisions live in `docs/decisions.md` (the 5 KB authoritative one). | repo root | Drift between sources of truth. | S — fold into `docs/`. |

## Notes on E2E "16 failing on CI"
I could not reproduce a current-state CI failure list from this checkout: the latest CI commits (`06e9463 fix(ci): inject Supabase env vars into E2E job`, `0b2c438 ci: add GitHub Actions workflow for typecheck, build, and security audit`) suggest the most recent fix targeted env-var injection — historically the dominant E2E-failure root cause. There is no `playwright-report/results.json` or `test-results/*.failed` artefact in the working tree to confirm a specific 16-test set. **Recommendation:** rerun `npm run test:e2e` against current `yolo-mode` and capture results before deciding whether the failures are still real, or whether the env-var fix already cleared them. (Local rerun was not performed in this audit since it would write to the test DB and the user only asked for a static report.)

## Issue counts
- Critical: **2 open + 1 resolved historical**
- High: **8**
- Medium: **12**
- Low: **8**
- **Total: 30 open + 1 resolved historical**
