import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function RolloutsPage() {
  return (
    <ComingSoonPage
      section="Operations"
      title="Rollout Engine"
      phase="Phase 3"
      description="Staged feature rollouts: cohort selection, ramp percentages, kill-switch, rollback."
    />
  );
}
