import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function LiveAccessPage() {
  return (
    <ComingSoonPage
      section="Support"
      title="Live Access (Impersonation)"
      phase="Phase 1E"
      description="Founder-only login-as-salon flow with audit-on-enter/exit, persistent banner, 30-min hard expiry, read-only mode option."
    />
  );
}
