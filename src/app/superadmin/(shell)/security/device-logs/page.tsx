import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function SecurityDeviceLogsPage() {
  return (
    <ComingSoonPage
      section="Security"
      title="Device Logs"
      phase="Phase 2"
      description="Per-user session history: device fingerprint, IP, geo, sign-in time. Source for forensic review during incidents."
    />
  );
}
