import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function BillingOverridesPage() {
  return (
    <ComingSoonPage
      section="Billing"
      title="Plan Overrides"
      phase="Phase 1F"
      description="Per-salon plan overrides for beta partners or manual upgrades. Currently lives inline under Salons until Phase 1F splits it out."
    />
  );
}
