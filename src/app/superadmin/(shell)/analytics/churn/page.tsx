import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function ChurnAnalyticsPage() {
  return (
    <ComingSoonPage
      section="Analytics"
      title="Churn Risk"
      phase="Phase 3"
      description="Per-salon churn signal: usage decay, payment failures, support touches. Read-only ranking."
    />
  );
}
