import { ComingSoonPage } from "@/components/superadmin/ComingSoonPage";

export default function SettingsPage() {
  return (
    <ComingSoonPage
      section="Settings"
      title="Settings"
      phase="Phase 1F"
      description="Superadmin user management (grant / revoke / change role), platform announcements admin, founder-only configuration."
    />
  );
}
