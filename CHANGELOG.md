# Changelog

All notable changes to NailIQ (project and documentation) are recorded here.

## 2026-05-02 (pre-launch — Task 5 empty salon + ADR)

- **Docs:** `current-focus.md` — failure-mode pre-launch blockers (see `637fd37`) marked DONE (service/staff delete guards, public booking RPC race, empty salon banner); timezone onboarding + demo cookie expiration deferred post-launch; next sprint stub **5/5–11/5/2026** (placeholders A/B/C).

- **Chore:** `.gitignore` — Playwright outputs grouped with comments; add **`blob-report/`** (merge-reports); **`playwright-report/`** and **`test-results/`** already ignored.

- **Receptionist Center — empty catalog UX (Task 5):** When the loaded salon has **no services** and/or **no staff**, **`ReceptionistCenter`** shows a **`nq-warning`** banner (title/message/**smart CTA**: **`setup/services`** when `services.length === 0`, else **`setup/staff`**) and passes **`addFormDisabled`** through **`WalkinQueueSidebar`** to **`WalkinAddForm`** (`disabled`: grayed fields, **`Add to queue`** disabled, submit short-circuited). i18n: **`receptionist.setupIncompleteBanner`** in **`src/shared/i18n/user`** EN/VI. Playwright: **`e2e/receptionist-center/empty-salon.spec.ts`** — **es-1** tenant `e2e-empty-salon-center` (0 services, 0 staff): banner + disabled submit + CTA → **`/setup/services`**; **es-2** `e2e-empty-salon-no-staff` (services, 0 staff): banner + CTA → **`/setup/staff`**. Run: `npx playwright test e2e/receptionist-center/empty-salon.spec.ts` (4 runs: chromium + mobile). Full suite: **`npm run test:e2e`** — **63 passed**, **1 skipped**; **`npm run typecheck`** + **`npm run build`**; smokes: `loadReceptionistCenterData`, `receptionistActions`, `publicBookingConflict` — PASS.

- **Decisions log — staff delete / terminal bookings:** New ADR at top of **`decisions-log.md`** (2026-05-02 afternoon): application-level **`staff_id` null** on **`cancelled`/`completed`** rows + **`preferred_staff_id`** clear before staff delete; active bookings still block. Inline reference in **`setupActions.ts`** `deleteStaff` before the detach UPDATE.

## 2026-05-02 (pre-launch — Task 1)

- **Setup — staff delete guard + e2e (Task 4):** `deleteStaff` counts **active** `bookings` for `staff_id` (`pending`, `confirmed`, `in_progress`, `waiting` — equivalent to status not in `cancelled`/`completed` for current enums). Returns **`staff_has_bookings`**; **`23503`** mapped the same. Before deleting the staff row, **terminal** bookings (`cancelled`/`completed`) get **`staff_id` null** and **`client_profiles.preferred_staff_id`** cleared for that id (FK-safe detach, not an `ON DELETE SET NULL` rule on `bookings`). Copy: **`setupErrors.staffHasBookings`** EN/VI; **`StaffSetupPanel`** uses `useUserLanguage` and maps **`staff_has_bookings`** + legacy **`in_use`**. Playwright: **`e2e/receptionist-center/staff-delete.spec.ts`** (slug `e2e-staff-delete`, `heading` wait uses **`exact: true`** to avoid **Add staff** substring match). Run: `npx playwright test e2e/receptionist-center/staff-delete.spec.ts` (4 passed: chromium + mobile).

- **Setup — service delete guard + e2e (Task 3):** `deleteService` (`setupActions.ts`) counts **non-cancelled** `bookings` for `service_id` before delete; returns **`service_in_use`** (Postgres **`23503`** still mapped the same). User copy via **`setupErrors.serviceInUse`** EN/VI (`src/shared/i18n/user`); **`ServicesSetupPanel`** maps codes through `getUserMessages` + `useUserLanguage`. Playwright: **`e2e/receptionist-center/service-delete.spec.ts`** (slug `e2e-service-delete`, demo cookie) — **sd-1** pending tomorrow booking blocks delete + message matches `đang được dùng|is used`; **sd-2** unused row removed from UI + DB. Run: `npx playwright test e2e/receptionist-center/service-delete.spec.ts`.

- **Receptionist Center — public vs desk race e2e (Task 2):** `e2e/receptionist-center/public-booking-race.spec.ts` — **race-1:** seed walk-in → service-role **assign** (same UPDATE shape as desk) → `create_public_booking` must return **`slot_conflict`** (PostgREST error or JSON body) and **no** row for the public client name; **race-2:** `TEST_E2E_*` appointment on **freeStaff** noon slot → full UI walk-in assign → `desk-action-message` + queue row still **waiting** + no `booking-block`. Uses `pickBookableDateYmd` (opening_hours + RPC **+2 min** lead) so slots stay valid. Run: `npx playwright test e2e/receptionist-center/public-booking-race.spec.ts` (4 worker runs: chromium + mobile).

- **`create_public_booking` overlap guard:** Migration `20260502130000_public_booking_conflict_check.sql` aligns pre-insert conflict semantics with Receptionist desk **`assignWalkinToSlot`** (status `NOT IN ('cancelled','waiting')`, standard overlap on `start_time_utc` / `end_time_utc`). Raises `slot_conflict` with SQLSTATE **`23P01`** before INSERT; RPC normalizes **`exclusion_violation`** to JSON `{success:false,"code":"slot_conflict"}` (PostgREST still returns structured body). Keeps **`pg_advisory_xact_lock`** per `(salon, staff)`. **Public booking UI:** `useBookingFlowState` shows **`bookingErrors.slotJustTaken`**, bumps **`getAvailableTimeSlots`** after conflict back navigation. **Smoke:** `npx tsx src/shared/dashboard/__tests__/publicBookingConflict.smoke.ts`.

## 2026-05-02 (V1 ship)

- **Receptionist Center V1 shipped** — separate operational screen at `/dashboard/[slug]/center` for salon front desk. Walk-in queue with add form (top 6 services + "more"), staff timeline grid (8am-8pm, 30min slots, sticky staff col + time header), assign flow (queue → click slot → confirmed booking with gold left border), 5s undo toast with countdown, drawer with status-aware actions (start/complete/cancel for any source). Realtime via Supabase postgres_changes + 8s poll fallback for demo. Status pill (calm/default/busy) + urgent badge (≥20min wait or vội/urgent/gấp note) auto-update via 60s tick. Migration `20260502120000_receptionist_center_schema.sql` adds `salons.timezone`, `bookings.{source, joined_queue_at, started_at, staff_request_note}`, relaxed null constraints for queue rows, extended `bookings_status_check` (waiting + in_progress + cancelled), tightened authenticated INSERT RLS to salon_members. Server actions: `addWalkinToQueue`, `assignWalkinToSlot` (race-condition safe with conflict check), `markWalkinInProgress`, `cancelWaitingWalkin`, `cancelDeskBooking`, `undoWalkinAssignment`, `updateBookingStatus` extended with pending/confirmed → in_progress. Helpers: `salonTime` (DST-safe TZ formatting), `conflictCheck` (pure overlap detection), `phoneFormat`, `queueUrgency`. Owner dashboard unchanged in semantics; today list filters extended to handle new statuses (waiting + cancelled hidden, in_progress shown). **`SalonOwnerDashboardMain`** adds **Front Desk / Tiếp Tân** (`salonDashboard.navReceptionistCenter`) to `/dashboard/[slug]/center` before Settings. 47/47 Playwright tests + 23 lib unit tests + 12/12 server action smoke tests pass. Product fix: z-index reorder in `StaffTimelineGrid` so assign-mode slot clicks reliably register above booking blocks.

## 2026-05-02 (evening)

- **Receptionist Center — Playwright e2e (final phase):** Added `e2e/receptionist-center/` specs + `helpers.ts` (service-role seed/cleanup for slug `e2e-receptionist-center`; **`NEXT_PUBLIC_SUPABASE_URL`** + **`SUPABASE_SERVICE_ROLE_KEY`** required — same as `e2e/helpers/db.ts`). Tests reuse **`nailiq-demo-slug`** demo cookie. **`StaffTimelineGrid`:** in assign mode, slot layer **`z-[3]`** and booking layer **`pointer-events-none`** so empty-slot clicks/hovers reach targets (ghost + assign under stacked blocks). **UI `data-testid`s:** `walkin-add-form`, `queue-item-{id}`, `queue-assign-{id}`, `queue-cancel-{id}`, `booking-block-{id}`, `ghost-block` (`data-state`), `assign-slot-{staffId}-{slotIndex}`, `staff-timeline-grid`, `status-pill` (`data-state`), `undo-toast`, `undo-toast-undo`, `booking-detail-drawer`, `booking-call-link`, `drawer-cancel-booking`, `desk-action-message`, `receptionist-center-loaded`. **`npm run test:e2e`:** 47 passed, 1 skipped (`grid-render` case 11 on **mobile** project — no fine-pointer hover). Remaining **manual** scope: real **iPad/Safari** pass + confirm **`tel:`** opens Phone app.

- **Receptionist Center V1 — QA posture:** Primary checklist coverage is **`npm run test:e2e`** (`e2e/receptionist-center/`); residual manual verification is **real-device** (see CHANGELOG bullet above). **P3:** Next.js 16 build warning — migrate `middleware` convention to **`proxy`** before Next 17 (non-blocking). **Cosmetic backlog:** `shakeMessage` toast has no CSS shake animation yet vs preflight “shake” wording.

## 2026-05-03

- **`create_public_booking` — salon timezone hours (production Sun/evening fix):** Migration `20260503140000_create_public_booking_salon_timezone_hours.sql` (**RPC v2.4**) evaluates **`opening_hours`** and **`booking_closed_dates`** using **`salons.timezone`** (`AT TIME ZONE`, same semantics as app `salonTime`), not UTC wall-clock. Adds **end-after-close** rejection aligned with **`assertSlotWithinOpeningHours`**. Invalid/unrecognized timezone falls back to **`America/Los_Angeles`**. **Smoke:** `publicBookingConflict.smoke.ts` — **9 PASS** (overlap + non-overlap + Sunday / edge cases + **salon-local `booking_closed_dates`**); **requires v2.4 applied** on the target Supabase project (pre-migration runs fail **`outside_hours`** on locally valid slots). **E2e:** `playwright.config.ts` **`timezoneId`** **`America/Los_Angeles`** (was UTC for old RPC wall-clock); **`CI=true npm run test:e2e`** — **73 passed**, **1 skipped**.

- **Docs — V1.1 backlog (founder Q1 review, afternoon):** Deferred pre-launch work on **add service** (multi-service / schema), **smart conflict suggestions**, and **custom price override** until post-beta activation triggers; V1 stays **time + staff + service replace** desk edit. Logged in **`decisions-log.md`** (2026-05-03 afternoon); **`PROJECT_BRAIN.md`** Decisions pointer; **`current-focus.md`** parking lot table (three rows + triggers).

- **Q1 desk edit booking shipped** — Receptionist drawer Edit button (pending/confirmed only) opens inline form with time + staff + service dropdowns; auto-recompute price + end_time from selected service; conflict check via shared `checkBookingConflict` (excludes self via `excludeBookingId`); status guard blocks edit for in_progress/completed/cancelled/waiting. Server: `editBooking(slug, input)` server action wrapping `performEditBooking` core logic in `editBookingCore.ts`; returns `{ ok: true, updated: SalonDashboardBooking }` or `{ ok: false, error: 'not_found' | 'invalid_status' | 'slot_conflict' | 'server_error', conflictWith?: string }`. UI: `EditBookingForm.tsx` controlled component with "no changes" detection (Save disabled when no diff); error message displays conflict client_name from server. i18n: `receptionist.edit.*` (EN + VI) production-quality strings; `drawer.editBooking` Edit/Sửa label. Tests: 5 e2e cases (`edit-booking.spec.ts`) + 5 smoke cases (`editBooking.smoke.ts`); 73 total e2e pass + 1 skipped baseline. Test stability fix: `moveMouseToAssignSlot` switched from `mouse.move` to `slot.focus()` (assign-slot buttons drive same hoveredSlot via `onFocus` as `onMouseEnter` — cross-row mouse path was flaky).

- **Docs — form button migration Task 3:** **`.cursorrules`** — verified **`WalkinAddForm`** / **`BookingDetailDrawer`** as reference templates (submit + footer actions on shared **`Button`**; pills / ✕ / scrim called out); added **Known gaps** list (**9** deferred files + **`SaveButton`** exception). **`current-focus.md`** P3 — foundation status (**2026-05-03**), remaining backlog pointer, visual notes (**`rounded-full`**, solid danger, **`text-nq-bg`**); separate P3 entry — **`SalonOwnerTodayBookings`** hydration mismatch (time display), observed e2e **2026-05-03**, fix when touching component or pre–V1 QA. **`PROJECT_BRAIN.md`** Design System — aligns with Known gaps + P3. Verify: **`npm run typecheck`** + **`npm run test:e2e`** — **73 passed**, **1 skipped**.

- **Receptionist — form actions on shared `Button` (Task 2):** **`WalkinAddForm`** submit uses **`Button`** `primary` + **`type="submit"`** with **`loading={submitting}`** and **`disabled={disabled}`** (setup incomplete); service grid pills, overflow list toggles, and **“more services”** stay native **`button`** (toggle / listbox pattern). **`BookingDetailDrawer`** primary CTA and **`drawer-cancel-booking`** use **`Button`** `primary` / **`danger`** with **`loading`** from action **`busy`**; backdrop scrim, header **✕**, and **`tel:`** link unchanged. **`npm run typecheck`**; **`npx playwright test e2e/receptionist-center/queue.spec.ts`** + **`npm run test:e2e`** — **73 passed**, **1 skipped**.

- **UI — shared `Button` `loading`:** Optional **`loading`** prop on `src/components/ui/Button.tsx` — inline **`border-current` / `border-t-transparent`** spinner (`size-4`, `motion-reduce:animate-none`), **spinner + existing label** (`gap-2`), **`aria-busy`**, **`disabled`** while loading, **`disabled:!opacity-70`** during load vs **`disabled:opacity-40`** when disabled but not loading (matches **`SaveButton`** saving-state weight). No Button Storybook/playground in repo. **`npm run typecheck`** + **`npm run test:e2e`** — **73 passed**, **1 skipped** (2026-05-03).

- **Receptionist Center Step 6c-2 — route + realtime + ticks:** `src/app/dashboard/[slug]/center/page.tsx` RSC loads `loadReceptionistCenterData` (`force-dynamic`, optional `?date=YYYY-MM-DD`, otherwise `salonToday` via `salons.timezone`); unauthorized matches other dashboard gates → `redirect("/register")`; metadata `Tiếp Tân · slug`, `robots: noindex`. **`ReceptionistCenter`:** Supabase **`postgres_changes`** on `public.bookings` (filter `salon_id`) when JWT session exists (`setAuth`, `onAuthStateChange`), else **8s** poll for demo/no-session desk; **`reloadCurrentDay()` only** on change (no optimistic merge). **`nowIso`** state + **60s** interval (now-line / queue urgency); **`nowIsoRef`** keeps `reloadCurrentDay` stable so realtime subscribe is not recreated every minute. **Undo toast:** `secondsRemaining` + **1s** interval auto-dismiss + **`UndoToast`** `showCountdown`.

- **Step 6c-1 follow-up:** `receptionistActions.smoke.ts` cases **9–12** (appointment `updateBookingStatus` mirror: pending→in_progress + `started_at`, pending→completed blocked, full lifecycle, confirmed→`cancelDeskBooking`). **`receptionist.actionErrors.not_found`** (EN/VI) + Receptionist toast maps owner `updateBookingStatus` `not_found` to it. **ADR:** cancelled `in_progress` keeps **`started_at`** — `decisions-log.md` 2026-05-02 (afternoon).

- **Receptionist Center Step 6c-1 — integrated client shell:** `src/components/receptionist/ReceptionistCenter.tsx` composes Steps 6a/6b, `loadReceptionistCenterDataAction` wrapper, **`receptionistActions`** + owner **`updateBookingStatus`** for desk flows; **`BookingDetailDrawer`** (grid tap + tel + Start / Complete / Cancel by status). **`src/shared/i18n/user`:** `receptionist` namespace (EN/VI). **`markWalkinInProgress`** retained for strict walk-in-only callers; **`cancelDeskBooking`** for grid cancellations. Undo toast shows static **5s** label until Step 6c-2 ticking; no Supabase realtime, no `nowIso`/`undo` countdown intervals, no `/center` route in this patch. **`receptionistActions`:** `cancelWaitingWalkin` (queue row remove → `cancelled`). **`WalkinQueueSidebar`:** queue item typing includes `service_id` for assign span.

- **Receptionist Center Step 6b — container components:** Added `DateSwitcher`, `WalkinAddForm`, `WalkinQueueSidebar`, `StaffTimelineGrid` under `src/components/receptionist/` (`"use client"`). Local state + interactions only; data and server work via props/callbacks (Step 6c parent). **salonTime:** `utcIsoToSalonMinutesFromMidnight`, `salonWallTimeToUtcIso` (DST-safe slot starts), `salonNowMinutes` refactored through shared `salonMinutesFromMidnightAt`. **globals.css:** `@keyframes nq-staff-busy-pulse` + `.nq-receptionist-busy-ring` (busy avatar; respects `prefers-reduced-motion`).

- **Receptionist Center Step 6a — presentational leafs:** Added client components `BookingBlock`, `NowLine`, `GhostBlock`, `StatusPill`, `UndoToast` under `src/components/receptionist/` (timeline block, now marker, ghost slot preview, queue pressure pill, post-assign undo toast). Styling uses `nq-*` tokens + `cn()` only; interactive targets ≥44px where applicable; no data loading or mutations. **Theme:** `colors.warning` in `src/shared/theme/tokens.ts` and `--color-nq-warning` in `:root` + `@theme` in `globals.css` for busy-state amber separate from accent gold.

- **Receptionist Center Step 5 — data loader:** Added `src/shared/dashboard/loadReceptionistCenterData.ts`: `getDashboardWriteClient` (lazy `import` from `setupActions` so date-format errors never call Next), re-read `salons` for `id|name|slug|timezone`, `salonDayRangeUtc` day window, parallel `staff` / `services` / walk-in queue / day `bookings` (statuses exclude `waiting`|`cancelled`; defensive filter on null slot fields). Optional `deps.resolveWrite` for tooling. **Smoke:** `src/shared/dashboard/__tests__/loadReceptionistCenterData.smoke.ts` — `npx tsx …` (service-role `resolveWrite` shim, invalid date, unauthorized, status invariants, queue vs DB).

- **Receptionist smoke (Step 4):** `src/shared/dashboard/__tests__/receptionistActions.smoke.ts` — manual verification with `SUPABASE_SERVICE_ROLE_KEY` + `NEXT_PUBLIC_SUPABASE_URL` from `.env.local`: mirrors mutations (cannot call `"use server"` actions in plain Node); optional `HARDCODED_*` / `NAILIQ_SMOKE_*` overrides; resolves tenant via slug default `nailiq-demo-slug`, else earliest salon (warn). Run: `npx tsx src/shared/dashboard/__tests__/receptionistActions.smoke.ts`.

- **Receptionist Center Step 4 — server mutations:** Added `src/shared/dashboard/receptionistActions.ts`: `addWalkinToQueue` (`slug` + `salonId` guard, `cleanPhone`, service ownership check, insert `waiting`/`walkin` + `joined_queue_at`); `assignWalkinToSlot` (staff+salon guards, duration from service join, `checkBookingConflict` then conditional `UPDATE … status=waiting` → `confirmed` with slot); `undoWalkinAssignment` (`confirmed` walk-in → `waiting`, clear slot — `already_started` when no matching row); `markWalkinInProgress` (`confirmed` walk-in → `in_progress` + `started_at`). Auth/write path mirrors `setupActions` via **`getDashboardWriteClient`** (demo cookie + service role vs member + RLS). Race narrowing: assign uses status guard + `lost_race` when the row moved.

- **Receptionist Center Step 3 — pure shared helpers:** Added `src/shared/lib/salonTime.ts` (format in salon TZ, `salonToday` / `salonDateOffset` / `salonDayRangeUtc` / `salonNowMinutes`, optional `nowIso` for tests; `Intl` only), `conflictCheck.ts` (staff + interval overlap; skips `cancelled` / `waiting`; optional `excludeBookingId`), `phoneFormat.ts` (`cleanPhone`, `formatPhone` US/CA), `queueUrgency.ts` (`minutesWaiting`, `isWalkinUrgent` with 20 min + keyword rule). TSX smoke tests in `src/shared/lib/__tests__/` (`conflictCheck`, `salonTime`, `queueUrgency`, `phoneFormat`). No React/Supabase in these modules.

- **Receptionist Center Step 2 — TS + owner dashboard booking shape:** Regenerated `src/lib/database.types.ts` from linked project. Extended `BookingStatus` / added `BookingSource`, `OWNER_TODAY_LIST_STATUSES`, `ACTIVE_GRID_STATUSES`; `SalonDashboardBooking` includes `source`, nullable `start_time_utc` / `client_phone`. `DASHBOARD_BOOKING_SELECT` + `mapDashboardBookingRow` map `source` and all valid statuses. `loadSalonOwnerDashboard` filters `.in("status", ACTIVE_GRID_STATUSES)` (excludes `waiting` / `cancelled` while keeping `completed` for today stats). `splitSalonDashboardBookings` uses full calendar `todayInWindow` for stats and filters the rendered **today** list with `OWNER_TODAY_LIST_STATUSES`; skips null `start_time_utc` in day math. `updateBookingStatus` allows `confirmed`/`pending`→`in_progress` (sets `started_at`) and completion paths incl. `in_progress`→`completed`. i18n: `statusInProgress` / `statusWaiting` / `statusCancelled`. Minor null-safe time/phone in owner dashboard components.

- **Supabase — Receptionist Center Step 1 (schema + RLS), revision:** Migration `20260502120000_receptionist_center_schema.sql` (replaces `20260503120000`): `salons.timezone` default `America/Los_Angeles`; `bookings.source` (+ check `appointment`|`walkin`), `joined_queue_at`, `started_at`, `staff_request_note`; nullable `start_time_utc`, `end_time_utc`, `staff_id`, `client_phone`; `bookings_insert_authenticated` requires `salon_id ∈ salon_members for auth.uid()` (replacing `with check (true)`). **Section 6:** `bookings_status_check` extended with `waiting` and `in_progress` for walk-in / chair lifecycle (applied to linked project + RLS smoke: `waiting` + `walkin` insert). **Not** in this migration: DB `bookings_no_overlap` changes (V1 overlap = server query check per pre-flight; optional EXCLUDE deferred). No redundant `source` backfill. Filename timestamp `20260502120000`. **Ops debt:** migration drift vs linked `schema_migrations` (`db push --linked` blocked); logged P2 in `current-focus.md` — resolve before V1 launch for repeatable deploys.

## 2026-05-02

- **Receptionist Step 6c-1 blocker fixes (approval):** `updateBookingStatus` now allows `pending`→`in_progress` and `confirmed`→`in_progress`, sets **`started_at`** on that transition (atomic `WHERE status = prior`), preserves existing transitions. **`BookingDetailDrawer`:** status-based **Start service** / **Mark complete** for all sources + **Cancel booking** (destructive + `confirm`) for non-terminal rows; **`ReceptionistCenter`** calls **`updateBookingStatus`** for primary actions and **`cancelDeskBooking`** (new in `receptionistActions`). User i18n: `drawer.markComplete`, `cancelBooking`, `cancelConfirm`; `actionErrors.invalid_transition`.

- **Landing hero (`/`):** `HomeLanding` refreshed copy (pricing headline via `landingH1Line1` + `landingH1Gold`, subline `landingBody1`, `autoLine`, `ctaSpeed`, updated CTA + microtrust), right-card label (`landingEarnedSub` **BOOKINGS YOU WON'T MISS** / VI equivalent); hero phone input removed (no landing `sessionStorage` phone prefetch). Nav wordmark: Inter, white semibold, not italic (`landingPhonePlaceholder` removed from user i18n). **`useUserLanguage`:** after mount, resolves `localStorage` `nailiq-user-lang` first, otherwise `navigator.language` `vi` prefix → Vietnamese (SSR-first English for hydration stability).

- **Docs — lint debt & decisions:** Added [current-focus.md](./current-focus.md) (active bugs, P3 ESLint inventory: 18 errors deferred post-launch) and [decisions-log.md](./decisions-log.md) entry recording deferral rationale; no application code changes.

- **Local dev canonical URL:** Use `NEXT_PUBLIC_SITE_URL=http://localhost:3000` in root `.env.local` while developing locally (Vercel production keeps its own env). Aligns inlined public site URL with the browser origin so dashboard booking links avoid SSR/client hydration mismatch.

- **Dashboard / register — booking URL hydration:** `getSiteUrlForClient` no longer uses `window.location.origin` on localhost; it always defers to `getSiteUrl()` / `NEXT_PUBLIC_SITE_URL` so SSR and the client match (`SalonOwnerDashboard` → `bookingAbsoluteUrl`). Register success copy-link uses the same `getSiteUrl()` path. Added committed `.env.example`, documented `NEXT_PUBLIC_SITE_URL` in `.env.test.local.example`, and `.gitignore` exceptions so those templates are not swallowed by `.env*`.

- **Supabase types:** Regenerated `src/lib/database.types.ts` from the linked Supabase project (`npx supabase gen types typescript --project-id fshmobzyjhmtvndobwsy`).

## 2026-04-29

- **Marketing home (`/`):** Rebuilt `HomeLanding` to match static reference `Landing.html`: deep navy `#050a15`, Playfair Display italic gold wordmark/headline accent + Inter body, glass cards, gradient CTA shimmer, live-style booking feed (client prepend loop), rotating social proof, three problem cards + closing CTA. Fonts via `next/font` on `page.tsx` (`--font-landing-inter`, `--font-landing-playfair`); scoped utilities in `globals.css` (`.landing-html-*`). Copy in `en.ts` / `vi.ts` under new `home.landing*` keys; phone + CTAs still route to `/register` with optional `sessionStorage` phone. Full-bleed layout without `ResponsiveShell`/`MobileStack` on home.

- **Dashboard — bookings Realtime vs demo:** Supabase `postgres_changes` on `bookings` respects RLS: **anonymous** clients cannot `SELECT` bookings (`bookings_select_anon` is `false`), so **demo cookie dashboard** (`demoMode`, no JWT) never receives live inserts — only **authenticated** salon members do. `SalonOwnerDashboard` now calls `getSession` + `realtime.setAuth` before subscribing and refreshes the Realtime JWT on `onAuthStateChange`; demo dashboards use **8s** poll instead of 30s. Dev: `console.warn` on subscribe `CHANNEL_ERROR` / `TIMED_OUT`.

## 2026-04-28

- **Dashboard — onboarding (Phase 2):** Extended `salons` with `address`, `salon_phone`, `opening_hours` (defaults Mon–Sat 09:00–18:00, Sun closed), and `profile_complete`; `staff.job_role` (`owner` \| `senior` \| `nail_tech`). Authenticated salon members gain INSERT/UPDATE/DELETE policies on catalog `services` and `staff`. Server actions in `setupActions.ts` verify access via demo cookie (`nailiq-demo-slug`) or `salon_members` + Supabase session; **`profile_complete`** = more than one service, more than one staff, and non-null address. **`SetupChecklist`**, setup nav link (**Setup** → services), gold banner while incomplete, and green readiness badge added to `SalonOwnerDashboardMain`. Routes: `/dashboard/[slug]/setup/{services,staff,hours,address}`. Migration: `20260430160000_phase2_salon_setup_and_catalog_write_rls.sql`.

- **Landing — owner sign-in path (PROMPT #015):** Top nav on `/` keeps layout: wordmark, centered **Owner Login** ↗ (`text-nq-muted/80`, hover `text-nq-primary`; **Login** on small screens) → `/register`, plus **Get started free**. Conversion block order: optional phone input → primary CTA → centered muted **Already have a salon? Sign in →** (`/register`). Register step shows *Returning owner? Enter your number to sign back in.* (EN/VI). Files: `HomeLanding.tsx`, `RegisterPageClient.tsx`, `src/shared/i18n/user/en.ts`, `vi.ts`.

- **Dashboard — progressive recovery email:** `salons` gains optional `email` and `email_verified` columns; authenticated owners may update their own salon row (RLS migration `20260428140000_salons_email_owner_update_policy.sql`). Non-blocking gold banner (`AddEmailBanner`) at `/dashboard/[slug]` when email is absent: inline validation, server action `addSalonEmail` (demo slug cookie vs `salon_members` + user session); dismiss persists in localStorage (`nailiq-email-banner-dismissed`) for 7 days; success toast then auto-hide. Helpers: `src/shared/lib/emailFormat.ts`; `resolveSalonForDashboard` exported for reuse.

## 2026-04-27

- **Supabase Auth & owner security:** Phone OTP via client `signInWithOtp` / `verifyOtp` when not in demo mode; `salon_members` table + authenticated RLS on `bookings` (select/update for member salons). Middleware (`src/middleware.ts`) refreshes session cookies and guards `/dashboard/*`. Registration completion uses `completeSalonRegistration` with a real `auth.getUser()` session; removed `registerFlow` `localStorage` dashboard gate. Demo path: `NEXT_PUBLIC_DEMO_OTP` + `DemoOtpModal`, `demoAuthBridge` session after custom `otps` verification.
- **Dashboard & booking UX:** Owner dashboard splits (`SalonOwnerDashboardMain`, stats, today list); masked phones and `#NQ-` booking references; `BookingFlow` split subcomponents + dynamic `canvas-confetti` import; step motion ~180ms. Shared types in `src/shared/types/index.ts`. Removed unused `AuthSheet`. New migration `20260428120000_salon_members_owner_rls.sql`. **PROJECT_BRAIN.md** documents auth and current modules.

## 2026-04-25

- **`/aggressive` two-column phone mock:** `PhoneDemo` in `src/app/aggressive/page.tsx` now shows an **app bar** (`phoneAppBar` + 💅), **service cards** (icon + name + **duration/price** from new i18n fields), a **horizontally scrollable** time-pill row (**four** slots including **4:30 PM**), and a **light bottom sheet** (drag handle, **success ring** with `niq-pop` + **SVG check** `niq-check` stroke draw, **+1 booking** line, **View schedule** button). New CSS in `globals.css` (`@keyframes niq-pop`, `niq-check`, `.niq-animate-aggr-*`, `.niq-check-path`). `AggressivePhoneDemo` still passes the first **three** time slots to the shared `PhoneFrame` hero. **PROJECT_BRAIN.md** updated.

- **High-urgency marketing page (`/aggressive`):** New single-column, mobile-first landing (separate from the Apple-style home): shock hero + FOMO line, **animated money loss counter** ($135 → $180 → $220, 2s), problem/solution lists, **PhoneFrame** hero demo (Pedicure / Gel / Manicure, Anna booking, **+1 new booking**), large three-line benefits, **rotating social proof**, no-barrier close, final CTA. **EN + VI** in `src/shared/i18n/user/aggressive.ts`; building blocks: `AggrSection`, `MoneyLossCounter`, `AggressivePhoneDemo`, `AggressiveLanding`. Sitemap + metadata. **PROJECT_BRAIN.md**.

- **Landing copy (conversion, no layout change):** Hero headline/subline, CTA sublines + **missed-call urgency**, input placeholder, and **rotating social proof** (Anna/Lisa lines) in **en** / **vi**; new **“autopilot”** block copy below the benefits list; in-phone **+1 new booking** micro line under the hero activity card (`PhoneHeroScreen` + i18n `phoneHeroMicroLine`). **Removed** static “100+ salons” string (`socialProof` key). Files: `src/shared/i18n/user/en.ts`, `vi.ts`, `MarketingHome.tsx`, `PhoneFrame.tsx`, `PhoneHeroScreen.tsx`, `MarketingPhoneAside.tsx`, **PROJECT_BRAIN.md**.

- **Public booking (`/[shop]`):** **Functional 3-step flow** — pick service, pick time, confirm; stub **`submitPublicBooking`**; demo catalog in **`getBookingCatalog`** (`src/shared/booking/`). **`BookingFlow`** client component; copy in `src/shared/i18n/booking/en.ts`. **PROJECT_BRAIN.md** updated. No landing changes.

- **Marketing phone aside:** Removed the **Upload your menu** CTA, file input, and success toast; `aiDemo` stays inactive. Dropped i18n keys `phoneUploadMenu` and `phoneAiMenuSuccess`. **PROJECT_BRAIN.md** updated.

- **Landing phone booking demo (3-step):** The scroll-linked **booking** scene is a full **Service → Time → Done** flow with **horizontal slide** transitions (framer-motion, transform/opacity), **gold** primary actions and step labels (`nq-*`), **navy** card surface (`nq-bg`), **iOS home indicator** (`IphoneHomeIndicator`), and **whileTap** scale on controls. Scene entry from **hero** uses **translateX** instead of scale. Copy in `en.ts` / `vi.ts` (`phoneBookingStep*`, `phoneBookingContinue`, `phoneBookingBookAnother`, etc.); `PhoneFrame` takes **`phoneBookingCopy`**. Files: `PhoneBookingView.tsx`, `IphoneHomeIndicator.tsx`, `phoneFrameTypes.ts`, `PhoneAiScanViews.tsx` (`PhoneSceneStack`), `MarketingPhoneAside.tsx`, `PhoneFrame.tsx`, **PROJECT_BRAIN.md**.

- **Interactive landing iPhone (scroll + AI menu demo):** **Scroll-linked scenes** via `IntersectionObserver` on benefits with `data-nq-benefit-scene` (`booking` | `ai`): **hero** (existing service/time/booking card loop), **booking** (date + time chooser), **AI Genesis** (menu scan idle). **framer-motion** for cross-fades and the gold **laser** sweep (1.5s) after **Upload your menu** (local image → blob URL, revoked on unmount). **Status bar** (live clock when motion allowed, signal + battery), **Navy + thin gold** bezel (`nq-*` tokens only). **`MarketingPhoneAside`** wraps the device + file CTA; **`useDocumentPhoneScene`**. i18n: `phoneUploadMenu`, `phoneAiMenuSuccess`, `phoneBookingSceneTitle`, `phoneAiSceneLabel`/`Hint`, `phoneMenuDemoRows`; benefit 2 re-titled **AI Genesis** with `phoneScene` keys. Dependency: `framer-motion`. Files: `src/components/ui/PhoneFrame.tsx`, `src/components/ui/phone/*`, `src/components/user/MarketingPhoneAside.tsx`, `MarketingHome.tsx`, `useDocumentPhoneScene.ts`, `en.ts`, `vi.ts`, **PROJECT_BRAIN.md**.

- **Phone mock polish (PROMPT #025):** **Staged flow:** service row only → **300ms** → time row on → **300ms** → `phoneActivity` updates (and card re-mount anim); **2.2s** hold with full two-row highlight, then same flow on the **next column**. **Card:** `nq-activity` now includes **0.98 → 1** scale (0.5s, subtle ease), `origin-center`. **Copy:** “**Anna booked** / **Jenny booked**” (EN); VI: **Anna vừa book** / **Jenny vừa book**. No extra UI. Files: `PhoneFrame.tsx`, `globals.css`, `en.ts`, `vi.ts`, **PROJECT_BRAIN.md**.

- **Phone mock content (PROMPT #024):** `PhoneFrame` in-screen area now shows a **stacked product mock**: service chips (**Pedicure / Manicure / Gel** order via `serviceStrip`), time chips (`phoneTimeSlots`: 3:00 / 3:30 / 4:00), and bottom **“New booking”** card (existing `phoneActivity` rotation, including **2:15 PM — Gel • $38**). Sync’d highlight on the top two rows (`~2.4s`); new `animate-nq-phone-tick` + existing card animation (≤5px). **Device shell unchanged.** `MarketingHome` passes `serviceStrip` and `timeSlots` from i18n. Files: `PhoneFrame.tsx`, `globals.css`, `en.ts`, `vi.ts`, `MarketingHome.tsx`, **PROJECT_BRAIN.md**.

- **Mobile hero polish (PROMPT #014):** iPhone-focused spacing and hierarchy on the foundation landing—**smaller mobile H1**, tighter hero stack, **subtle** wordmark (`text-base`, gold opacity), **mobile vs desktop CTA** (`ctaMobile` / `cta` in EN + VI). **`DesktopSplit`** now takes **`head` + `aside` + `rest`** so **PhoneFrame** sits **after the hero on mobile** (higher in the fold) while **desktop** stays two-column. **`ResponsiveShell`** slightly reduced **vertical padding** on small breakpoints; **`MobileStack`** unchanged for **`pb-safe`**. Files: `MarketingHome.tsx`, `DesktopSplit.tsx`, `ResponsiveShell.tsx`, `en.ts`, `vi.ts`. **PROJECT_BRAIN.md** (*iPhone hero polish*). No full-page redesign.
- **Auto push system (PROMPT #013):** Added `scripts/auto-push.js` (chokidar watch, 30s debounce, ignores `.next` / `node_modules` / `.git` / `logs`, mutex so pushes do not overlap, `git status` before commit, optional path-based short commit messages, fallback `auto update`). `npm run auto-push` in `package.json`. Documented in **PROJECT_BRAIN.md** under *Auto Push System*.
- **Brand system lock (PROMPT #012):** Defined and locked NailIQ **brand role** (headline = focus, CTA = second, wordmark = anchor), **logo type scale** (no `text-2xl`+ on landing wordmark; mobile `text-base`–`text-lg`, desktop `text-lg`–`text-xl`), **color usage** (gold only for CTA / highlight / focus; palette fixed in tokens and `globals.css`), **calm premium feel**, and **distributed branding** (header, card `Badge`, footer “Powered by”). Updated `src/shared/theme/tokens.ts` (JSDoc), `src/components/user/MarketingHome.tsx` (wordmark, spacing, tagline cap vs headline, `footerPoweredBy`), `src/shared/i18n/user/en.ts` + `vi.ts`, **PROJECT_BRAIN.md**, **CHANGELOG.md**, **.cursorrules** (`BRAND RULES`) to prevent future UI inconsistency. No layout or page redesign.
- **SEO + AI discovery (PROMPT #010):** Global metadata in `src/app/layout.tsx` (title template and default, description, keywords, Open Graph, Twitter, robots, `metadataBase` via `getSiteUrl()`). Landing `metadata` + JSON-LD graph in `src/app/page.tsx` (`getLandingJsonLd` in `src/shared/seo/jsonLd.ts`: `SoftwareApplication`, `Organization`, `WebSite`). Semantic landing copy: `heroHeadline`, `seoIntro`, benefit list with `h2`/`h3` in `MarketingHome`; i18n keys in `src/shared/i18n/user/en.ts` and `vi.ts`. `src/app/robots.ts`, `src/app/sitemap.ts` (`/`, `/register`), `public/ai.txt`, `public/llms.txt`. Register stub `src/app/register/page.tsx`. Booking route `generateMetadata` in `src/app/[shop]/page.tsx`. Helpers `src/shared/seo/site.ts`. SEO rules appended to `.cursorrules`.
- **Language system (PROMPT #008):** Split by surface — **user UI** (foundation home) uses `src/shared/i18n/user/en.ts` + `vi.ts`, `getUserMessages`, `EN | VI` toggle (`UserLanguageToggle`), and `useUserLanguage` (persist `nailiq-user-lang` in `localStorage`, default English, `document.documentElement.lang` sync). **Booking UI** is English only: `src/shared/i18n/booking/en.ts`, `src/app/[shop]/page.tsx` stub, `BookingDocumentEn` to keep `html` `lang` English; no toggle and no user-locale on that route. `PhoneFrame` accepts optional `serviceStrip` for translated rotating labels. Files: `src/shared/i18n/user/*`, `src/shared/i18n/booking/en.ts`, `src/shared/lib/useUserLanguage.ts`, `src/components/user/MarketingHome.tsx`, `UserLanguageToggle.tsx`, `src/app/[shop]/*`, `src/app/page.tsx` → `MarketingHome`, `src/components/ui/PhoneFrame.tsx`.
- **Responsive adaptive UI (PROMPT #007):** Mobile-first `ResponsiveShell`, `MobileStack` (max width mobile column, `pb-safe` / optional `stickyCta` + `pb-safe-cta`), and `DesktopSplit` (`lg+` two columns, max `1180px`). `tokens.ts`: `breakpoints` and `layout`. `globals.css`: `--max-nq-*`, `--pad-nq-*`, `html`/`body` `overflow-x: hidden`, `pb-safe` utilities. Foundation home reflow: iPhone order logo → copy → trust → CTA → input → phone; desktop: copy column + larger headline + `PhoneFrame` with desktop gold wash. `layout.tsx` body `min-h-dvh` / `overflow-x-hidden`. Files: `src/components/layout/ResponsiveShell.tsx`, `MobileStack.tsx`, `DesktopSplit.tsx`, `src/shared/theme/tokens.ts`, `src/app/globals.css`, `src/app/page.tsx`, `src/app/layout.tsx`.
- Initial setup
- Auto memory system created
- **Foundation UI:** Added branded design tokens (`src/shared/theme/tokens.ts`) and extended global Tailwind theme (`src/app/globals.css`) for premium dark + gold. Added `cn()` helper (`clsx` + `tailwind-merge`). Shipped base UI: `Button` (primary / secondary / ghost / danger, sm–lg with 56px `lg` CTA), `Card` (dark glass, gold-tinted border, `shadow-nq-card`), `Input` (focus + error focus rings from theme), `Badge`, `Section` (content width + optional label), and `PhoneFrame` (iPhone-style preview). Home page is a non-production design preview only. Added `npm run typecheck`.
- **Foundation UI (conversion pass):** Tighter, emotional copy; centered ~`480px` column with vertical centering; CTA, trust, and value props. `Button` micro-interactions (hover/press scale). `Input` 52px min height, richer gold focus glow, 300ms transitions. `Card` luxury gradient wash + `ring` + combined `shadow-nq-card-luxury`. `PhoneFrame` backlit gold orb (CSS), rotating service list + subtle content pulse—no `useEffect`, no animation libraries. `prefers-reduced-motion` respected for ambient animations.
- **Hero conversion upgrade (PROMPT #005):** Headline and CTA refocused on booking outcomes; social proof under CTA; sub-input copy for SMS link delivery. `PhoneFrame` service strip: 10s CSS loop (~2.5s per name) with handoff opacity (still no `useEffect`/animation libs). `Button` press scale to `0.98` to match spec.
- **Apple-level UI (PROMPT #006):** **Liquid glass** system in `src/app/globals.css` (`.glass`, `.glass-strong`, `.glow-gold`, `.text-shimmer` + `shimmer`, `.fade-in` + `fadeIn`); `prefers-reduced-motion` fallbacks. **Button** — `use client`, gold glow shadow, 150ms transitions, 100ms click opacity flash (timer cleanup on unmount). **Card** — glass + glow, top gradient wash, more padding, `rounded-3xl`, hover lift. **Input** — combined focus ring + layered gold glow, `transition-all` 200ms. **PhoneFrame** — `setInterval` 2.5s for `Pedicure` / `Gel` / `Manicure` (effect cleanup, interval skipped when `prefers-reduced-motion`), `fade-in` on label change, extra backlit layer. **Home** — top ambient gold spotlight; **hero** `text-shimmer` on “NailIQ” only. Removed unused `nq-service-rotate` keyframes. Still Tailwind + CSS only (no new animation packages).
