{
  "name": "nailiq-voice-bridge",
  "private": true,
  "type": "module",
  "description": "Twilio Voice <-> OpenAI Realtime bridge for the NailIQ AI receptionist (persistent WS; not deployable on Vercel serverless).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": { "ws": "^8.18.0" },
  "devDependencies": { "@types/ws": "^8.5.10", "typescript": "^5.6.0", "vitest": "^2.1.0" }
}
